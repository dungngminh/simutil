name: simutil
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

dependencies:
  cli_util: ^0.4.2
  nocterm: ^0.5.1
  path: ^1.9.0
  process_run: ^1.3.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
